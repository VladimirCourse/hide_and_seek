name: hide_and_seek
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'


dependencies:
  flutter:
    sdk: flutter

  # ioc
  provider: ^6.0.0

  # state management
  bloc: ^8.0.0
  flutter_bloc: ^8.0.0

  # ui
  cupertino_icons: ^1.0.2

  # platform tools
  flutter_blue_plus: ^1.3.1
  flutter_ble_peripheral: ^1.1.1 

  # sound_stream:
  #   git:
  #     url: https://github.com/AlexanderHorielko/flutter-sound-stream.git
  #     ref: android-fix

  mic_stream: ^0.6.2 
  # audioplayers: ^1.1.1 

  # util
  freezed_annotation: ^2.1.0
  rxdart: ^0.27.5 
  easy_debounce: ^2.0.2+1 
  tuple: ^2.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  freezed: ^2.1.0+1
  build_runner: ^2.2.0




flutter:
  uses-material-design: true
